Ideas:
    switch changing data structures to Lenses (floormaps, entities and its members)
    change GameState to monadic (could make its use easier)

ToDo:
    instance Random Floor where
    instance Random Hero where

Bugs:
